This directory stores the compressed English Destiny 2 manifest tables used by
server routes.

Run `pnpm manifest:update` to refresh `manifest.json` and `en/*.json.gz`.
GitHub Actions also refreshes these files every Tuesday at 17:00 UTC.
